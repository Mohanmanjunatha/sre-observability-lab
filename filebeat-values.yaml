daemonset:
  enabled: true

deployment:
  enabled: false

secretMounts: []

filebeatConfig:
  filebeat.yml: |
    filebeat.inputs:
      - type: container
        paths:
          - /var/log/containers/*.log

    output.elasticsearch:
      hosts: ["http://host.minikube.internal:9200"]
